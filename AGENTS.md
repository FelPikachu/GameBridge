# GameBridge 项目规则

## 项目定位

GameBridge 是面向 SteamOS / Steam Deck 的独立、Provider 驱动的第三方游戏聚合与兼容运行平台。项目通过官方渠道及许可证兼容的外部组件提供游戏库、安装、兼容环境和 Steam 快捷方式能力。

不得复制其他插件的源码、UI、品牌资源或专有数据，也不得暗示 Valve、平台方或游戏发行商对本项目提供了授权、背书或官方支持。

## 每次修改前的保护锁

- 读取 `docs/verified-baseline.md`，不要从聊天记录猜测已验收功能。
- 修 Bug、改功能、重构、打包或发布时，加载 `.codex/skills/gamebridge-regression-guard/SKILL.md`。
- 先标出受影响的平台、游戏、功能和共用链路，再改代码。
- 已验收行为只能在有明确用户需求和替代验收证据时改变，不得悄悄回退。
- 自动测试通过不等于真机通过；真机未验证的结论必须写“待真机验证”。

## 架构与安全边界

- 依赖方向保持为：Decky UI → Application API → Domain Core → Provider SDK → Platform Adapters。
- Provider 不直接操作 UI；UI 不直接执行外部 CLI。
- 外部命令使用参数数组执行，不拼接未经控制的 shell 命令字符串。
- 认证材料不得写入核心数据库；日志不得包含密码、Token、Cookie、授权码、设备标识或完整认证请求体。
- 不绕过 DRM、授权、登录、地区限制或反作弊，不引入依赖这类绕过的补丁或启动器。
- Provider 和区域之间需要隔离的数据目录、Prefix、认证状态不得混用。
- 不删除用户外部游戏目录、Wine Prefix、凭证或其他用户数据，除非任务明确授权且删除范围已经核实。

## 修改、测试与验收

- 保留用户已有修改，不重置、覆盖或清理与当前任务无关的文件。
- 遵循最小改动原则；不为“去 AI 痕迹”改写稳定代码，不在修当前问题时顺带重构无关代码。
- 修改 Provider、启动链、安装状态机、存储、数据库、凭证处理、清理器或分发包时，增加针对性失败路径和回归测试。
- 实机验证通过的修复必须固化到源码并增加回归测试，不得只留在手工配置、外部脚本或聊天记录中。
- 一个功能只有在插件源码包含实现、相关回归测试通过，并在按发布流程生成的安装包中再次验证后，才能标记为“已真机验收”。
- 不把用户机器的绝对路径、账号状态、凭证、临时 App ID 或一次性环境数据写死到插件中。

## 桌面 ZIP 交付规则

- 给用户桌面交付新的 GameBridge ZIP 前，先在项目 `releases/` 中保留一份同内容备份，并确认两份 SHA-256 一致。
- 新包校验成功后，桌面只保留最新的 GameBridge ZIP；将桌面其他旧 GameBridge ZIP 移入回收站，不直接永久删除。
- 清理范围仅限桌面上的 GameBridge ZIP。不得删除清理工具、其他安装包、其他桌面文件或项目内的发布备份。
